#include "holberton.h"

/**
 * mul - multiplies two integers
 *@a: Multiply a with b
 *@b: Multiply b with a
 * Return: the result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
